<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>
	
	<!-- <properties resource="db.properties"></properties> -->
	
	<settings>
		<setting name="jdbcTypeForNull" value="NULL"/><!-- 널허용 -->
		<setting name="callSettersOnNulls" value="true"/><!-- 값넘겨라 -->
		<!--<setting name="mapUnderscoreToCamelCase" value="true"/>--> <!-- 카멜표기법 -->
	</settings>
	
	<typeAliases>
		<typeAlias type="kr.or.nextit.member.model.Member" alias="Member"/>
		<typeAlias type="kr.or.nextit.board.model.Board" alias="Board"/>
		<typeAlias type="kr.or.nextit.common.file.model.FileItem" alias="FileItem"/>
	</typeAliases>
	
	
<!-- id=development로 기본사용 -->
  <!-- <environments default="development">

    <environment id="development">
      <transactionManager type="JDBC"/> --> <!-- JDBC/MANAGED -->
      <!-- <dataSource type="POOLED"> --> <!-- 자체적인 POOL지원 -->
       <!--  <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>

    <environment id="test">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>
    
    <environment id="product">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>

  </environments> -->

  <!-- <mappers>
    <mapper resource="mapper/MemberMapper.xml"/>filepath경로
    <mapper resource="mapper/BoardMapper.xml"/>
    <mapper resource="mapper/CommonMapper.xml"/>
    <mapper resource="mapper/FileItemMapper.xml"/>
  </mappers> -->

</configuration>